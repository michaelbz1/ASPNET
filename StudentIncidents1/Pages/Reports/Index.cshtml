@page
@model StudentIncidents1.Pages.StudentIncidents.ReportIndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                StudentLastName
            </th>
            <th>
                StudentFirstName
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StudentIncident[0].Incident)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StudentIncident[0].IncidentDate)
            </th>

        </tr>
    </thead>
    <tbody>
<<<<<<< HEAD
@foreach (var item in Model.StudentIncident) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Student.StudentFirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Incident.IncidentName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IncidentDate)
            </td>
            <td>
            </td>
        </tr>
}
=======
        @{ int myCounter = 0;
            int Total = 0;
            bool start = true;
            bool form_visible = false;
            string currentItem = "";
            string pastItem = "";
        }

                @foreach (var item1 in Model.StudentIncident.GroupBy(s => s.Student.StudentFirstName))
                {
                    @foreach (var item in item1)
                    {
                        pastItem = currentItem;
                        currentItem = item.Incident.IncidentName.ToString();

                        if (start)
                        {
                            //var replacement = source.Replace("mountains", "peaks");
                            pastItem = currentItem;
                            start = false;
                        }

                        if (pastItem.Equals(currentItem))
                        {
                            myCounter++;
                            form_visible = false;
                        }
                        else
                        {
                            Total = myCounter;
                            myCounter = 1;
                            form_visible = true;
                        }

                        @if (form_visible)
                        {
                            <tr>
                                <td>
                                    <button class="btn btn-outline-success">@Total</button>
                                </td>
                            </tr>

                        }

                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Student.StudentLastName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Student.StudentFirstName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Incident.IncidentName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.IncidentDate)
                            </td>
                        </tr>

                      }
                  }
>>>>>>> 17faccf800ae236f8d1c0e93a7fa724eab6b1eeb
    </tbody>
</table>