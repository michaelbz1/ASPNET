@page
@model StudentIncidents.Pages.Parents.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Parent[0].ParentFirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Parent[0].ParentLastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Parent[0].ParentPhone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Parent[0].ParentEmail)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Parent) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ParentFirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ParentLastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ParentPhone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ParentEmail)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
